# How to update the chump package list on the website
- Add the top level run `make mac`.
- then run `./builddir-arm64/scripts/chump_doc <path to chump-packages repo> <output dir>`

This will generate all of the package listings for https://chuck.stanford.edu/release/chump
